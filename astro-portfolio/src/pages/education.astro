---
import BaseLayout from '../layouts/BaseLayout.astro';
const base = import.meta.env.BASE_URL;
---
<BaseLayout title="教育 | 李卓衡" description="Education - NYU, Xi'an Conservatory">
  <meta http-equiv="refresh" content={`0;url=${base ? base + 'about/' : '/about/'}`} />
  <p class="redirect-msg">
    <span class="content-zh">教育内容已合并至<a href={`${base}about/`}>关于</a>页，正在跳转…</span>
    <span class="content-en">Education is now on the <a href={`${base}about/`}>About</a> page. Redirecting…</span>
  </p>
</BaseLayout>
<style>
  .redirect-msg { margin: 2rem 0; color: var(--muted); }
  .redirect-msg a { color: var(--accent); }
</style>
