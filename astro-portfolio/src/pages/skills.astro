---
import BaseLayout from '../layouts/BaseLayout.astro';
const base = import.meta.env.BASE_URL;
---
<BaseLayout title="技能 | 李卓衡" description="Skills - Python, SQL, ETL, BI">
  <meta http-equiv="refresh" content={`0;url=${base}about/`} />
  <p class="redirect-msg">
    <span class="content-zh">技能已合并至<a href={`${base}about/`}>关于</a>页，正在跳转…</span>
    <span class="content-en">Skills are now on the <a href={`${base}about/`}>About</a> page. Redirecting…</span>
  </p>
</BaseLayout>
<style>
  .redirect-msg { margin: 2rem 0; color: var(--muted); }
  .redirect-msg a { color: var(--accent); }
</style>
