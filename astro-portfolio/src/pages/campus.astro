---
import BaseLayout from '../layouts/BaseLayout.astro';
const base = import.meta.env.BASE_URL;
const educationUrl = base ? `${base}education/` : '/education/';
---
<BaseLayout title="校园 | 李卓衡" description="Campus - now on Education page">
  <meta http-equiv="refresh" content={`0;url=${educationUrl}`} />
  <p class="redirect-msg">
    <span class="content-zh">校园与志愿已移至<a href={educationUrl}>教育</a>页西安音乐学院卡片下，正在跳转…</span>
    <span class="content-en">Campus & volunteering is now on the <a href={educationUrl}>Education</a> page under Xi'an Conservatory. Redirecting…</span>
  </p>
</BaseLayout>
<style>
  .redirect-msg { margin: 2rem 0; color: var(--muted); }
  .redirect-msg a { color: var(--accent); }
</style>
