# 西安音乐学院详情页图片 / Xi'an Conservatory page images

把西安音乐学院相关图片放在此文件夹（如 `campus.jpg`、`performance.jpg`），页面中用 `img('文件名')` 引用。

若暂时没有图片，请在 `src/pages/education/xian.astro` 里注释掉或删除对应的 `<figure>...</figure>`，避免显示裂图。
